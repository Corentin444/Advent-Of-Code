#include <iostream>
#include <fstream>
#include <vector>
#include <string>
#include <regex>
#include <map>

int main()
{
    std::ifstream infile("input.txt");
    std::string line;
    int ans = 0;
    
    while (std::getline(infile, line))
    {
        
    }

    std::cout << ans;
}